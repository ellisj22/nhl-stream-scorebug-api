# nhl-live-scorebug-overlay
A Python script that pulls live NHL game data from the NHL public API and generates a text file used by OBS Studio to power a live scoreboard overlay during streams.  Built specifically for Buffalo Sabres watch streams.
